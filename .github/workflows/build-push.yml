name: Build and Push PrivateVault Image

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Image
        run: |
          cd runtime
          docker build -t ghcr.io/${{ github.repository_owner }}/privatevault:pilot-v2 .

      - name: Push Image
        run: |
          docker push ghcr.io/${{ github.repository_owner }}/privatevault:pilot-v2
