# Agent Framework Interoperability

This directory contains validated adapters and test harnesses proving
PrivateVault compatibility with major agent frameworks.

Supported:
- LangChain
- AutoGen
- LlamaIndex
- CrewAI
- Semantic Kernel

Each integration generates cryptographically verifiable evidence
bundles compatible with PrivateVault verification.

Run tests using:

    make verify_all
